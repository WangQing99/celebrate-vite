/**
 * @description: 异常相关的枚举
 */
export enum ExceptionEnum {
    // 页面不能访问
    PAGE_NOT_ACCESS = 403,

    // 页面没有找到
    PAGE_NOT_FOUND = 404,

    // error
    ERROR = 500,

    // net work error
    NET_WORK_ERROR = 10000,
}
